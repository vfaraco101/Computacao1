#include<stdio.h>
#include<math.h>

int main(){
    float altura, largura, area, tinta;

    printf("Digite a altura da parede: ");
    scanf("%f", &altura);
    printf("Digite a largura da parede: ");
    scanf("%f", &largura);
    area = (altura * largura);
    tinta = (area/2);
    tinta = ceil(tinta);


    printf("Area da parede: %.1f \nQuantidade de tinta necessaria: %.0f", area, tinta);
}
